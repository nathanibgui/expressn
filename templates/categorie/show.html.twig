{% extends 'base.html.twig' %}

{% block title %}Categorie{% endblock %}

{% block body %}
<div class="container">
    <div class="row">
        <div class="col-md-12 mx-auto">
        <h1>Categorie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Nom</th>
                <td>{{ categorie.nom }}</td>
            </tr>
        </tbody>
    </table>

        <a class="btn btn-primary mr-2" href="{{ path('app_categorie_index') }}">Retour</a>

        <a class="btn btn-success" href="{{ path('app_categorie_edit', {'id': categorie.id}) }}">Modifier</a>

        </div>
    </div>
</div>
{% endblock %}
